<%= form_with(model: measurement_datum, local: true) do |form| %>
  <% if measurement_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(measurement_datum.errors.count, "error") %> prohibited this measurement_datum from being saved:</h2>

      <ul>
        <% measurement_datum.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <% if form.object.graph_time %>
    <div class="field">
      <%= form.label :graph_time %>
      <%= form.datetime_select :graph_time %>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
